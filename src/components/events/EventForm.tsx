// ... existing code ...

// Change to default export
export default function EventForm({ 
  open, 
  onClose, 
  onSubmit, 
  initialData, 
  title 
}: EventFormProps) {
  // ... existing code ...
}
