// ... existing code ...

// Change to default export
export default function SpeakerForm({ 
  open, 
  onClose, 
  onSubmit, 
  initialData, 
  title 
}: SpeakerFormProps) {
  // ... existing code ...
}
