// ... existing code ...

// Change to default export
export default function OrganizationForm({ 
  open, 
  onClose, 
  onSubmit, 
  initialData, 
  title 
}: OrganizationFormProps) {
  // ... existing code ...
}
