export interface AccountFormData {
  firstName: string;
  lastName: string;
  username: string;
  accountType: string;
}

export interface AccountState {
  firstName: string;
  lastName: string;
  username: string;
  accountType: string;
}
